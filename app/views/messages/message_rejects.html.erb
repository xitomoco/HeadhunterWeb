<div class="container">
  <div class="text-center">
    <h1 class="display-3"><%= Profile.human_attribute_name('rejections') %></h1>
    <br/>
  </div>

  <% if @profile.user.comment_of_rejects.empty? %>
    <div class="text-center">
      <h3><%= Profile.human_attribute_name('empty') %></h3>
    </div>
  <% end %>

  <%= render partial: 'nav_msg' %>

  <%= render partial: 'rejects' %>
</div>